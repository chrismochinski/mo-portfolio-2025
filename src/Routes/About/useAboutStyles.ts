import { createStyles } from '@mantine/core';

export const useAboutStyles = createStyles(() => ({
  aboutWrapper: {
    zIndex: 5,
    textAlign: 'center',
    p: {
      textWrap: 'balance',
    },
  },
}));
